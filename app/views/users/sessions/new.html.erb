<html> 
<!-- body class="~"のようにbodyで設定してしまうと、application.scssで設定したデザインにも反映されて大きさや位置が変わったりするのでapplication.scssで設定したものをそのまま別のサイトに反映したかったら、body class="~"やid="~"などの設定をしない -->
<body>
 <div id="user_link">
  <div id="user_container">

    <h1 class="mycompany_logo">SiteK(株)</h1>
      <div id="log-in_box">
        <h2 class="log-in">Log in</h2>
        <div class="social-login">
          <button class="social-btn google">Google</button>
          <button class="social-btn twitter">X (Twitter)</button>
          <button class="social-btn apple">Apple</button>
        </div>
        <div class="formbox">
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  
            <div class="field">
              <span class="log-in_char">  <%= f.label :name %><br /> </span>
              <div class="log-in_input">
                <%= f.text_field :name, autofocus: true, autocomplete: "name" %>
              </div>
            </div>
  
            <div class="field">
              <span class="log-in_char">  <%= f.label :email %><br /> </span>
              <div class="log-in_input">
                <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
              </div>
            </div>

            <div class="field">
              <span class="log-in_char">  <%= f.label :password %><br /> </span>
              <div class="log-in_input">
                <%= f.password_field :password, autocomplete: "current-password" %>
              </div>
            </div>

            <% if devise_mapping.rememberable? %>
              <div class="field_selsect">
                <div class="log-in_checkbox">
                  <%= f.check_box :remember_me %> 
                  <%= f.label :remember_me %>
                </div>
              </div>
            <% end %>

            <div class="actions">
                <%= f.submit "Log in" %>
            </div>
          <% end %>
        </div>
      </div>
  </div>
 </div>
</body>
</html>
<%= render "users/shared/links" %>
